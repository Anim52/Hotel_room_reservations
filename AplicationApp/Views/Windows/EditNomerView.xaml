<Window x:Class="AplicationApp.Views.Windows.EditNomerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AplicationApp.Views.Windows"
        mc:Ignorable="d"
        Title="EditNomerView" Height="640" Width="800" Background="#55687B"
        WindowStyle="None" ResizeMode="NoResize">
    <Grid Margin="10">
        <StackPanel Margin="20,0,20,0" VerticalAlignment="Top">
            <Button HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10,0,0,0" Width="80" Content="✕" Background="#1E293B" Foreground="Red" Click="Close_btn_Click"/>
            <TextBlock Text="Номер" FontSize="18"/>
            <TextBox Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <TextBlock Text="Этаж" FontSize="18"/>
            <TextBox Text="{Binding Floor, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <TextBlock Text="Фото" FontSize="18"/>
            <Button Content="Изменить фото" Command="{Binding SelectImageCommand}" Margin="0,5,0,10" Background="#1E293B" Width="180" Height="50"/>

            <TextBlock Text="Стоимость" FontSize="18"/>
            <TextBox Text="{Binding Cost, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <TextBlock Text="Описание" FontSize="18"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <TextBlock Text="Тип номера" FontSize="18"/>
            <ComboBox ItemsSource="{Binding TypeNumderList}" 
              SelectedItem="{Binding SelectedTypeNumder}" 
              Margin="0,5,0,10"/>

            <Button Content="Сохранить"
            Command="{Binding EditNomerCommand}"
            
            HorizontalAlignment="Center" Background="#1E293B" Width="180" Height="50"
            Margin="0,10,0,0"/>
        </StackPanel>

    </Grid>
</Window>
